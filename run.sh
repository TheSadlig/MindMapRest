#!/bin/sh

# Create virtual env ?
# Install Requirements
# -> Flask
# -> pip install -U Flask-SQLAlchemy
# Launch tests
# Set env variables
# Start Server 

# Add options (tests only, start only etc)
