#!/bin/sh

# Create virtual env ?
# Install Requirements
# -> Flask
# -> pip install -U Flask-SQLAlchemy
# -> pip install nose2
# Launch tests
# Set env variables
# Start Server 

# Add options (tests only, start only etc)
